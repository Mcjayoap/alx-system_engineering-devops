#!/bin/env bash

# List the content of the current directory
for file in $(ls | grep -v "^\."); do
  # Extract the part of the file name after the first dash
  name=${file#*-}
  # Display the processed file name
  echo $name
done

